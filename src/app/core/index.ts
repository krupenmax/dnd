export interface CharacterRace {
	id: number;
	name: string;
	code: string;
}

export interface CharacterClass {
	id: number;
	name: string;
	code: string;
}

export interface CharacterLevel {
	id: number;
	name: string;
}
